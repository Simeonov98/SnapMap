{% extends 'base.html.twig' %}

{% block content %}
    <div class="container mt-2 bg-light rounded">
        <div class="row text-center">
            <p class="text-justify p-4">{{ output }}</p>

            <p class="text-justify p-4">{{ outputzip }}</p>
        </div>
        <div class="row text-center">
            <p class="text-justify p-4">{{ output2 }}</p>
        </div>
        <div class="row">
            <div class="col-md-12 text-center" style="padding-bottom: 10px">
                <a class="btn btn-success m-2 text-center" href="{{ path('download',{'id':fileId}) }}">Свали</a>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12 text-center" style="padding-bottom: 10px">
                <a class="btn btn-success m-2 text-center" href="{{ path('app_homepage_homepage') }}">Начало</a>
            </div>
        </div>


    </div>
{% endblock %}